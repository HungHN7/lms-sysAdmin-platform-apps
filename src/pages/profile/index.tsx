import React from 'react';
import { ContentProfile, HeaderProfile } from 'src/containers/profile';

const ProfilePage = () => {
  return (
    <div>
      <HeaderProfile />
      <ContentProfile />
    </div>
  );
};

export default ProfilePage;
